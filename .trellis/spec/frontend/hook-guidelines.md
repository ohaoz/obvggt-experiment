# Hook Guidelines

> Not applicable until a React-like UI exists.

---

## Current Rule

This workspace does not currently use React hooks, custom hooks, or browser-side data fetching layers.

Do not create:

- `use*` hooks
- client-side fetch wrappers
- UI state sharing helpers

unless the task explicitly introduces a frontend application.
